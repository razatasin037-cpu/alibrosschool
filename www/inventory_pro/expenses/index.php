<?php $pageTitle='Expenses'; require_once __DIR__.'/../includes/header.php';
if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$st=$pdo->prepare("INSERT INTO expenses(title,category,amount,expense_date,notes,created_by) VALUES(?,?,?,?,?,?)");$st->execute([trim($_POST['title']),trim($_POST['category']),max(0,(float)$_POST['amount']),$_POST['expense_date'],trim($_POST['notes']),$_SESSION['user']['id']]);flash('success','Expense added.');redirect('index.php');}
$rows=$pdo->query("SELECT * FROM expenses ORDER BY expense_date DESC,id DESC")->fetchAll();
?>
<div class="toolbar"><div><h1 class="page-title">Expenses</h1><div class="muted">Track operating expenses for management reporting.</div></div></div>
<div class="grid grid-2"><div class="card"><h3>Add Expense</h3><form method="post"><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><div class="form-grid"><div class="form-group full"><label>Title</label><input class="input" name="title" required></div><div class="form-group"><label>Category</label><input class="input" name="category" placeholder="Rent, salary, transport..."></div><div class="form-group"><label>Amount</label><input class="input" type="number" step="0.01" min="0" name="amount" required></div><div class="form-group"><label>Date</label><input class="input" type="date" name="expense_date" value="<?=date('Y-m-d')?>" required></div><div class="form-group full"><label>Notes</label><textarea class="textarea" name="notes"></textarea></div></div><button class="btn btn-warning" style="margin-top:12px">Save Expense</button></form></div>
<div class="card table-wrap"><table class="table"><thead><tr><th>Date</th><th>Title</th><th>Category</th><th>Amount</th></tr></thead><tbody><?php foreach($rows as $r):?><tr><td><?=e($r['expense_date'])?></td><td><?=e($r['title'])?></td><td><?=e($r['category']??'')?></td><td><strong><?=money($r['amount'])?></strong></td></tr><?php endforeach;?></tbody></table></div></div>
<?php require_once __DIR__.'/../includes/footer.php';?>
